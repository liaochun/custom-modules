# assets

Drop screenshots and diagrams here, then link them from the docs.

Suggested captures (all from the running platform — sanitize any real names/data first):

- `dashboard.png` — the 17-section inventory/ops dashboard
- `schedule.png` — `/admin/schedule` week view
- `master-items.png` — the editable Master Items grid with the change-log drawer open
- `kiosk.png` — the PIN clock-in screen
- `sync-status.png` — the header sync dot / authoritative-table toggle

Reference them in a doc like:

```markdown
![Ops dashboard](../assets/dashboard.png)
```

Keep images free of customer data, staff PII, real emails, and any credentials.
